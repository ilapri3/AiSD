str=input()
ans_str=''
ans=''
for i in range(len(str),0,-1):
    if str[i-1]!=' ':
        ans_str+=str[i-1]
    else:
        break
for i in range(len(ans_str)):
    if ans_str[i].isalpha():
        ans+=ans_str[i]
print(ans[::-1])
