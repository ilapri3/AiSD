str=input()
ans=[]
ans_array=[]
for i in range(len(str)):
    if str[i].isalpha():
        count=str.count(str[i])
    ans.append(str[i]*count)
    count=0
new_list=set(ans)
for el in new_list:
    if el =='':
        pass
    else:
        ans_array.append(el)
print(sorted(ans_array))
